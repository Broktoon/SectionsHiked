// All Supabase database calls live here.
// Requires auth.js to be loaded first (provides window.sb).
//
// Add functions as features are built: getHikeSegments, addHikeSegment,
// deleteHikeSegment, getBadges, awardBadge, etc.
